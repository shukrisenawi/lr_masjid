<x-app-layout><x-slot name="header"><div class="flex items-center justify-between"><h2 class="text-2xl font-semibold text-slate-900">Rekod Kematian</h2><a href="{{ route('death-records.create') }}" class="rounded-lg bg-slate-900 px-4 py-2 text-sm text-white">Tambah Rekod</a></div></x-slot><div class="py-6"><div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8"><div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">@foreach($deathRecords as $record)<div class="rounded-2xl bg-white p-4 shadow-sm ring-1 ring-slate-100">@if($record->image_path)<img src="{{ asset('storage/'.$record->image_path) }}" class="mb-3 h-40 w-full rounded-xl object-cover">@endif<p class="font-semibold text-slate-900">{{ $record->full_name }}</p><p class="text-xs text-slate-500">{{ $record->nickname ?: '-' }}</p><p class="mt-2 text-sm text-slate-600">{{ $record->death_time->format('d/m/Y H:i') }}<br>{{ $record->place }} | {{ $record->village?->name ?? '-' }}</p><div class="mt-4 flex gap-2"><a href="{{ route('death-records.edit', $record) }}" class="rounded bg-slate-100 px-2 py-1 text-xs">Edit</a><form method="POST" action="{{ route('death-records.destroy', $record) }}" onsubmit="return confirm('Padam rekod?')">@csrf @method('DELETE')<button class="rounded bg-rose-100 px-2 py-1 text-xs text-rose-700">Padam</button></form></div></div>@endforeach</div></div></div></x-app-layout>
