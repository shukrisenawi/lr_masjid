<x-app-layout><x-slot name="header"><div class="flex items-center justify-between"><h2 class="text-2xl font-semibold text-slate-900">Hebahan</h2><a href="{{ route('announcements.create') }}" class="rounded-lg bg-slate-900 px-4 py-2 text-sm text-white">Tambah Hebahan</a></div></x-slot><div class="py-6"><div class="mx-auto grid max-w-7xl gap-4 px-4 sm:grid-cols-2 lg:grid-cols-3">@foreach($announcements as $item)<div class="rounded-2xl bg-white p-4 shadow-sm ring-1 ring-slate-100">@if($item->image_path)<img src="{{ asset('storage/'.$item->image_path) }}" class="mb-3 h-40 w-full rounded-xl object-cover">@endif<p class="font-semibold text-slate-900">{{ $item->title }}</p><p class="mt-2 text-sm text-slate-600">{{ \Illuminate\Support\Str::limit($item->body, 120) }}</p><p class="mt-2 text-xs text-slate-500">{{ optional($item->published_at)->format('d/m/Y') ?: '-' }}</p><div class="mt-4 flex gap-2"><a href="{{ route('announcements.edit', $item) }}" class="rounded bg-slate-100 px-2 py-1 text-xs">Edit</a><form method="POST" action="{{ route('announcements.destroy', $item) }}" onsubmit="return confirm('Padam hebahan?')">@csrf @method('DELETE')<button class="rounded bg-rose-100 px-2 py-1 text-xs text-rose-700">Padam</button></form></div></div>@endforeach</div></div></x-app-layout>
