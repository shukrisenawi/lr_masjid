@csrf
<label class="mb-1 block text-sm">Anak Khariah</label><select name="member_id" class="w-full rounded-lg border-slate-300">@foreach($members as $member)<option value="{{ $member->id }}" @selected((string) old('member_id', $assignment->member_id ?? '') === (string) $member->id)>{{ $member->full_name }}</option>@endforeach</select>
<label class="mb-1 mt-4 block text-sm">Jenis Bayaran</label><select name="payment_type_id" class="w-full rounded-lg border-slate-300">@foreach($paymentTypes as $type)<option value="{{ $type->id }}" @selected((string) old('payment_type_id', $assignment->payment_type_id ?? '') === (string) $type->id)>{{ $type->name }} ({{ $type->method }})</option>@endforeach</select>
<label class="mb-1 mt-4 block text-sm">Tarikh Penetapan</label><input type="date" name="assigned_at" value="{{ old('assigned_at', isset($assignment) && $assignment->assigned_at ? $assignment->assigned_at->format('Y-m-d') : '') }}" class="w-full rounded-lg border-slate-300">
<label class="mb-1 mt-4 block text-sm">Status</label><select name="status" class="w-full rounded-lg border-slate-300"><option value="Aktif" @selected(old('status', $assignment->status ?? 'Aktif') === 'Aktif')>Aktif</option><option value="Tidak Aktif" @selected(old('status', $assignment->status ?? '') === 'Tidak Aktif')>Tidak Aktif</option></select>
<div class="mt-6 flex gap-2"><button class="rounded-lg bg-slate-900 px-4 py-2 text-sm text-white">Simpan</button><a href="{{ route('payment-assignments.index') }}" class="rounded-lg bg-slate-100 px-4 py-2 text-sm">Batal</a></div>
